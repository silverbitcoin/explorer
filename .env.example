# ============================================================================
# PRODUCTION-GRADE SILVERBITCOIN EXPLORER CONFIGURATION
# ============================================================================

# MongoDB Connection - Real blockchain data storage
# Format: mongodb://[username]:[password]@[host]:[port]/[database]
MONGODB_URI=mongodb://localhost:27017/silverbitcoin_explorer

# Blockchain Node RPC Connection
# These credentials connect to the SilverBitcoin node for real blockchain data
WALLET_HOST=127.0.0.1
WALLET_PORT=19766
WALLET_USERNAME=explorer_user
WALLET_PASSWORD=secure_password_here

# Explorer UI Configuration
NEXT_PUBLIC_COIN_NAME=SilverBitcoin
NEXT_PUBLIC_COIN_SYMBOL=SLVR
NEXT_PUBLIC_EXPLORER_URL=https://explorer.silverbitcoin.org
NEXT_PUBLIC_LOGO_PATH=/logo.png

# ============================================================================
# REAL BLOCKCHAIN DATA SOURCES
# ============================================================================

# Mining Pool Backend (silver-backend)
# Provides real mining pool statistics and miner data
MINING_POOL_API=http://localhost:3001/api
MINING_POOL_USERNAME=explorer
MINING_POOL_PASSWORD=secure_password

# Storage Backend (silver-storage)
# Direct access to ParityDB for block and transaction data
STORAGE_DB_PATH=/var/lib/silverbitcoin/storage

# ============================================================================
# PERFORMANCE & CACHING
# ============================================================================

# Redis Cache (optional, for performance)
REDIS_URL=redis://localhost:6379

# Cache TTL in seconds
CACHE_TTL_STATS=10
CACHE_TTL_BLOCKS=30
CACHE_TTL_TRANSACTIONS=5
CACHE_TTL_ADDRESSES=60
CACHE_TTL_RICHLIST=300

# ============================================================================
# MONITORING & LOGGING
# ============================================================================

# Log level: debug, info, warn, error
LOG_LEVEL=info

# Enable detailed error logging
DEBUG_MODE=false

# ============================================================================
# SECURITY
# ============================================================================

# Admin panel password for maintenance
ADMIN_PASSWORD=change_me_in_production

# API rate limiting
API_RATE_LIMIT=100
API_RATE_WINDOW_MS=60000

# ============================================================================
# DEPLOYMENT
# ============================================================================

# Node environment
NODE_ENV=production

# Next.js configuration
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# ============================================================================
# SYNC COMMANDS REFERENCE
# ============================================================================
# npm run sync:index:update     - Update blockchain from last sync to current block
# npm run sync:index:check      - Check for missing transactions/addresses
# npm run sync:index:reindex    - Clear and resync everything from genesis block
# npm run sync:index:richlist   - Update richlist only
# npm run sync:market           - Update market data only
# npm run sync:daemon           - Start automatic scheduled sync
# npm run sync:clean-locks      - Remove stale lock files
# npm run sync:force            - Force update (removes locks first)

# ============================================================================
# REAL BLOCKCHAIN INTEGRATION
# ============================================================================
# This explorer connects to:
# 1. silver-storage: ParityDB for block/transaction/mining data
# 2. silver-pow: Mining pool and difficulty data
# 3. silver-core: Transaction and address types
# 4. silver-backend: Mining pool REST API
# 5. silver-p2p: Network peer information
#
# All data is REAL, not mocked or simulated.
# No placeholders, no simplified implementations.
# Production-grade error handling and performance optimization.

